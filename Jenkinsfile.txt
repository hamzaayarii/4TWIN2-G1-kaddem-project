pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Pull code from the specific GitHub branch
                git url: 'https://github.com/hamzaayarii/4TWIN2-G1-kaddem-project.git', 
                    branch: 'oussamakaddech-4Twin2-G1', 
                    credentialsId: 'github-credentials' // omit if public repo
            }
        }
        stage('Build') {
            steps {
                // Clean and compile the project
                sh 'mvn clean compile'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                // Run SonarQube analysis with hardcoded token
                withSonarQubeEnv('SonarQube') {
                    sh 'mvn sonar:sonar -Dsonar.host.url=http://192.168.56.10:9000 -Dsonar.login=squ_e82b239048eee2a2eb5899bcb8fd3b3188e6b49d'
                }
            }
        }
        stage('Deploy') {
            steps {
                sh 'mvn deploy -DskipTests'
            }
        }
    }
}
